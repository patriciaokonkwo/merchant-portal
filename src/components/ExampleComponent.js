// Add scope attributes and aria-labels
const ExampleComponent = () => {
  return (
    <div scope="row" aria-label="Example component">
      Example
    </div>
  );
};

export default ExampleComponent;